Implementazione del metodo initiliaze(scs[]) in AccumulatorResultSet per modificare lo score del result set ottenuto
Implementation of the unimplemented method initiliaze(scs[]) of class AccumulatorResultSet, to allow me to modify the result set's score obtained


VSM fa il vector space model calcolando le norme al momento
The VSM class retrieve the file using the classi vector space model, computing the L2 norm online.

VectorNormalizer è un programma java a parte che usa Preprocessing per creare le norme a priori.
VectorNormalizer it's a stand-alone class which compute the L2 norm of the documents a priori


VSMOffline usa le norme a priori che vanno ovviamente calcolate prima
The VSM class retrieve the file using the class vector space model using the L2 norm pre-computed by VectorNormalizer

TF_IDF2 è il trec_model modificato usando il TF_IDF standard senza la norma di Robertson, si setta dal properties e va in Matching Model insieme agli altri (non sono sicuro del nome della cartella ma si trova facile)
TF_IDF2 is a matching model obtained modifying the Terrier's standard TF_IDF model, deleting the Robertson's norm. It's possible to set it from the .properties of Terrier. It necessites to be in the src folder of the others matching models


Scusate le note brevi, più in la se ci son dubbi ne parliamo su FB ;-)
PS: initialise si implementa semplicemente con una copia del vettore degli score, capirete meglio leggendo il listato, poi cmq ci becchiamo
e ne parliamo
